# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Page.index()

# Login page
GET     /login                     	controllers.Page.login(email:String ?= "")
POST    /login                     	controllers.AuthController.login()

# Process Model XML files
GET     /process/*processID			controllers.Page.xmlProcess(processID)
POST    /uploadProcess				controllers.Page.uploadProcess()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /webjars/*file             	controllers.WebJarAssets.at(file)
